--- configure.ac.orig	2011-04-29 13:50:58.584119115 -0600
+++ configure.ac	2011-04-29 13:52:02.113119164 -0600
@@ -122,6 +122,7 @@
 			AC_SUBST(XPIDL_BIN)
 			XUL_IDLDIR="`$PKG_CONFIG --variable=idldir libxul`"
 			AC_SUBST(XUL_IDLDIR)
+            AC_DEFINE(MOZ_NO_MOZALLOC, ["For Mozilla Firefox 4"])
 		;;
 		geany)
 			if test "${HAVE_LIBZG}" != "yes" ; then
