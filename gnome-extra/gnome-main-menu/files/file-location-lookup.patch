--- libslab/slab-gnome-util.c	2006-10-02 09:28:32.000000000 -0600
+++ libslab/slab-gnome-util.c.new	2006-10-26 12:52:59.000000000 -0600
@@ -169,35 +169,8 @@
 	gchar *package_name;
 	gint retval;
 	GError *error;
-
-	argv = g_new (gchar *, 6);
-	argv[0] = "rpm";
-	argv[1] = "-qf";
-	argv[2] = "--qf";
-	argv[3] = "%{NAME}";
-	argv[4] = g_filename_from_uri (gnome_desktop_item_get_location (desktop_item), NULL, NULL);
-	argv[5] = NULL;
-
-	error = NULL;
-
-	if (!g_spawn_sync (NULL, argv, NULL, G_SPAWN_SEARCH_PATH, NULL, NULL, &package_name, NULL,
-			&retval, &error))
-	{
-		g_warning ("error: [%s]\n", error->message);
-
-		return NULL;
-	}
-
-	g_free (argv[4]);
-	g_free (argv);
-
-	if (error)
-		g_error_free (error);
-
-	if (!retval)
-		return package_name;
-	else
-		return NULL;
+	package_name = "test";
+	return package_name;
 }
 
 gboolean
